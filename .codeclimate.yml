engines:
  rubocop:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        ruby:
          mass_threshold: 60
ratings:
  paths:
    - lib/**
exclude_paths:
  - spec/**/*
